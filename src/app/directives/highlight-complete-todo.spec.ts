import { HighlightCompleteTodo } from './highlight-complete-todo';

describe('HighlightCompleteTodo', () => {
  it('should create an instance', () => {
    const directive = new HighlightCompleteTodo();
    expect(directive).toBeTruthy();
  });
});
